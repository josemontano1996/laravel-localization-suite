The app must always have a fallback locale, in case no default config locale was given, if not present will throw error.

The app.supported_locales must be filled with at least one supported locale, if not a error will be thrown