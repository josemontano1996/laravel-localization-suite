services:
    laravel.test:
        build:
            context: "./vendor/laravel/sail/runtimes/${PHP_VERSION:-8.4}"
            dockerfile: Dockerfile
            args:
                WWWGROUP: "${WWWGROUP}"
        image: "sail-${PHP_VERSION:-8.4}/app"
        extra_hosts:
            - "host.docker.internal:host-gateway"
        ports:
            - "${APP_PORT:-80}:80"
            - "${VITE_PORT:-5173}:${VITE_PORT:-5173}"
        environment:
            WWWUSER: "${WWWUSER}"
            LARAVEL_SAIL: 1
            XDEBUG_MODE: "${SAIL_XDEBUG_MODE:-off}"
            XDEBUG_CONFIG: "${SAIL_XDEBUG_CONFIG:-client_host=host.docker.internal}"
            IGNITION_LOCAL_SITES_PATH: "${PWD}"
            OCTANE_WORKERS: 1
            SESSION_DRIVER: array
            CACHE_STORE: array
            QUEUE_CONNECTION: sync
            DB_CONNECTION: sqlite
            DB_DATABASE: ":memory:"
            SUPERVISOR_PHP_COMMAND: "/usr/bin/php -d variables_order=EGPCS artisan octane:swoole --host=0.0.0.0 --port=80"
            OCTANE_SERVER: swoole
            LOCALIZATION_DRIVER: swoole
        volumes:
            - "../../../../:/var/www"
        working_dir: /var/www/tests/Integration/runtimes/context
        networks:
            - sail
        depends_on: {}
networks:
    sail:
        driver: bridge
